def func_1(cost_price, selling_price):
    return cost_price == selling_price
    #State of the program after the return statement: cost_price and selling_price are integers representing the cost price and selling price of an item respectively
#Overall this is what the function does: The function `func_1` accepts two parameters: `cost_price` and `selling_price`, both of which are integers representing the cost price and selling price of an item respectively. Therefore, the functionality of the function `func_1` is to accept two integer parameters, `cost_price` and `selling_price`, which represent the cost price and selling price of an item respectively.
cost_price and selling_price are integers representing the cost price and selling price of an item respectively

